

<div class="container span-10 offset-1">
    <div class="row">
        <div class="center">
            <h3> {{ event.name }}</h3>
        </div>


    </div>

    <div class="row">
        <div>
            <p>{{ event.description }}</p>
            <p>Du :{{ event.date_debut }} au : {{ event.date_fin }}</p>
            <p>Lieu : {{ event.lieu }} </p>
        </div>
    </div>


    <div class="row">
        <h5>Epreuves :</h5>
        {% for epreuve in tabEpreuve%}
            <div>
                <p>{{ epreuve.nom }}</p>
            </div>

        {% endfor %}
    </div>

    <div class="row">
        <div>
            <p> Nombre de participants : {{ event.nb_participant }}</p>
            <p> Les inscriptions sont actuellement : {{ event.etat }}</p>
            <p> Vous pouvez désomais télécharger la liste des participants pour les différentes épreuves :</p>
        </div>

        <div>
            {% for epreuve in tabEpreuve%}
                <div>
                    <p>{{ epreuve.nom }}</p>
                    <button>Participant à l'épreuve</button>
                    <button>ajouter les résultat de l'épreuve</button>
                </div>
            {% endfor %}
        </div>

    </div>

</div>